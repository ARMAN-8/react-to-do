import React from 'react';

const SearchPanel = () => {
    return(
        <input
        className="form-control search-input border border-gray-400 rounded-lg"
        type="text"
        placeholder="Поиск по записям"/>
    )
};

export default SearchPanel;
